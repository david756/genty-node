const express = require("express");
const router = express.Router();

const chat = require("../controllers/chat.controller");

//router.post("/url-function", chat.function);

module.exports = router;
