const express = require("express");
const router = express.Router();

const setting = require("../controllers/setting.controller");

//router.post("/url-function", setting.function);

module.exports = router;
